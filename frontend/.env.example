# Public runtime configuration for the React app.
# These values are not server secrets, but they should still be set explicitly per environment.

VITE_API_BASE_URL=https://localhost:7052
VITE_WEB3FORMS_ACCESS_KEY=replace-with-your-web3forms-access-key